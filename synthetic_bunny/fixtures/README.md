# Synthetic Fixtures

This folder contains curated, versioned synthetic datasets used as stable
inputs for demos and presentation plots.

- Keep this folder small and intentional.
- Put ad-hoc/generated run outputs in `synthetic_bunny/output/` instead.
- `synthetic_bunny/output/` is treated as ephemeral and ignored by git.
